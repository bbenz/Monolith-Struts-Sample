db.url=jdbc:postgresql://localhost:5432/skishop
db.username=skishop
# db.password should be injected at deploy time (env var DB_PASSWORD or property replacement).
db.password=${DB_PASSWORD}
db.pool.maxActive=20
db.pool.maxIdle=5
db.pool.maxWait=10000
app.timezone=UTC
smtp.host=localhost
smtp.port=1025
smtp.username=
smtp.password=
mail.from=no-reply@skishop.local
